#include "main.h"

/**
 * _memset - does nothing
 * @s: destination string
 * @b: source string
 * @n: input integer
 * Return: Always 0
 */
char *_memset(char *s, char b, unsigned int n)
{
        return (s);
}
